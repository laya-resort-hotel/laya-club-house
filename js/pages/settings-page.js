export function renderSettingsPage(user, context = {}) {
  const pushStatus = context.pushEnabled ? 'Push alerts connected' : (context.pushReason || 'Push alerts not enabled yet');
  return `<section class="page-section"><article class="card profile-card"><p class="eyebrow">Profile</p><h2>${user.displayName || 'Member'}</h2><p class="muted">${user.role || 'member'}${user.roomNo ? ` • Room ${user.roomNo}` : ''}</p><div class="info-row" style="margin-top:12px"><span class="badge">Mode ${context.mode || 'production'}</span><span class="badge">${context.projectId || 'unconfigured'}</span><span class="badge">${pushStatus}</span></div>${user.profileMissing ? '<p class="warning-copy">This account signed in to Firebase successfully, but `users/{uid}` was not found yet.</p>' : ''}</article></section><section class="setting-list"><article class="setting-item"><span>Edit profile</span><span>›</span></article><article class="setting-item"><span>Change password</span><span>›</span></article><article class="setting-item"><span>Change language</span><span>›</span></article><article class="setting-item"><span>Install app</span><span>›</span></article><article class="setting-item"><span>Enable push alerts</span><span>›</span></article><article class="setting-item"><span>Check version</span><span>›</span></article><article class="setting-item"><span>Clear cache</span><span>›</span></article><article class="setting-item"><span>Logout</span><span>›</span></article></section>`;
}
