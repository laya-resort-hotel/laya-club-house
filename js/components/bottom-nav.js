import { isAdminLike, isStaffLike } from '../utils/helpers.js';
export function getTabsForUser(user = {}) { const tabs = [{ key: 'home', label: 'Home', icon: '⌂' }, { key: 'member', label: 'Member', icon: '◫' }, { key: 'redeem', label: 'Redeem', icon: '🎁' }, { key: 'chat', label: 'Chat', icon: '✉' }, { key: 'notifications', label: 'Alerts', icon: '🔔' }]; if (isStaffLike(user.role)) tabs.push({ key: 'scan', label: 'Scan', icon: '⌁' }); if (isAdminLike(user.role)) tabs.push({ key: 'admin', label: 'Admin', icon: '▦' }); tabs.push({ key: 'settings', label: 'Settings', icon: '⚙' }); return tabs; }
export function renderBottomNav(activeRoute, user, badges = {}) { return getTabsForUser(user).map((tab) => { const badgeCount = Number(badges?.[tab.key] || 0); return `<button class="nav-btn ${tab.key === activeRoute ? 'is-active' : ''}" data-route="${tab.key}"><span class="nav-icon-wrap"><span>${tab.icon}</span>${badgeCount > 0 ? `<span class="nav-badge">${badgeCount > 99 ? '99+' : badgeCount}</span>` : ''}</span><span>${tab.label}</span></button>`; }).join(''); }
